# AI 自动阅卷浏览器插件需求规格说明书 (PRD)

## 1. 文档概述
本文档旨在定义一款基于 Chrome 浏览器的自动阅卷插件。该插件通过捕获网页中的学生作答区域，利用飞桨 (PaddlePaddle) OCR 识别文本，并结合 DeepSeek AI 模型进行智能评分，实现自动化填分与提交逻辑。

## 2. 目标用户
* 线上教育平台阅卷老师
* 学校课后作业批改人员

## 3. 功能需求

### 3.1 配置模块
用户需在插件配置面板（建议使用侧边栏 Side Panel）输入以下信息：
* **API 配置：**
    * 飞桨 OCR API 地址及密钥（AppID / API Key / Secret Key）。
    * DeepSeek API 调用地址及 API Key。
* **阅卷控制：**
    * **【手动确认分数】开关：** 复选框，默认不勾选。勾选后，AI 给出分数并填入框中后将结束阅卷，由人工提交，用来审核评分结果的准确与否。
    * **【阅卷份数】输入框：** 限制输入大于 0 的整数。
* **文件系统：**
    * **【临时文件目录】选择：** 调用浏览器目录选择器，用于存放阅卷过程中的节点截图。
    * **【阅卷结束自动删除临时文件】开关：** 复选框，默认不勾选。

### 3.2 阅卷执行逻辑
点击【开始阅卷】按钮后，系统按顺序循环执行以下任务：

1.  **节点定位：** 在当前活跃页面中寻找 `id="subjectmark_content_svg"` 的容器。
2.  **录制截图 (Media Capture)：**
    * 调用浏览器 **MediaDevices API (getDisplayMedia)** 或插件特有的 **chrome.tabCapture**。
    * 将捕获的视频流绘制到 Canvas。
    * 根据目标 DOM 节点的坐标和尺寸进行精准裁剪，生成图片数据。
3.  **文件保存：** 将截图保存至用户选定的“临时文件目录”。
4.  **文本识别 (OCR)：** 调用飞桨接口，对截图中的学生作答内容进行文字提取。
5.  **智能评分 (AI)：**
    * 整合：[标准答案] + [OCR 识别的学生作答文本]。
    * 提示词 (Prompt)：要求 DeepSeek 返回具体分数值及简短理由。
6.  **分数回填：** 找到页面中 `class="score-input"` 的输入框，填入 AI 给出的分数。
7.  **文件重命名：** 完成当前份数评分后，将该份截图重命名为 `第n份-m分.png`。
8.  **流程推进：**
    * 若**未勾选**【手动确认分数】：自动点击 `class="submit-button"` 提交，并进入下一份阅卷。
    * 若**已勾选**：停止自动流程，等待人工点击提交。
9.  **清理收尾：** 达到预设阅卷份数后，若勾选了删除开关，则调用 File System Access API 清空并删除临时目录内的文件。

## 4. 技术方案评估与选型

### 4.1 截图方案：录制 API (Media Capture API)
* **原理：** 通过 `navigator.mediaDevices.getDisplayMedia` 获取屏幕流。
* **优势：** 能够捕获到 SVG 内部复杂的渲染效果，且在跨域图像限制下比 `html2canvas` 更稳定。
* **实现要点：** 需将流转为 `video` 标签，在 `requestAnimationFrame` 中截取单帧并根据 `getBoundingClientRect()` 进行 Canvas 裁剪。

### 4.2 文件交互：File System Access API
* **权限处理：** 使用 `window.showDirectoryPicker()` 获取 `FileSystemDirectoryHandle`。
* **持久化：** 利用该句柄可以实现对本地文件的静默写入、重命名 (`move`) 和删除 (`removeEntry`)。

### 4.3 核心 API 集成
* **Paddle OCR：** 负责将图片中的手写体或印刷体转为结构化文本。
* **DeepSeek AI：** 负责语义理解与分值计算。

## 5. UI/UX 设计建议
* **位置：** 推荐使用 Chrome Side Panel (侧边栏)，避免用户点击页面其他位置时配置窗口消失。
* **状态反馈：** 提供进度条或日志输出，实时显示“正在识别第 N 份...”、“AI 正在评分...”等状态。

## 6. 非功能性需求
* **安全性：** API Key 需本地加密存储在 `chrome.storage.local` 中。
* **稳定性：** 需处理网络超时、DOM 节点未找到等异常情况，并提供“停止阅卷”紧急按钮。
